### 加载状态

使用 `loading` 属性启用加载状态，使用 `loadingProps` 属性或 `loading` 插槽自定义加载状态

<preview path="@docs/component/table/demos/state-loading.vue"></preview>

### 空状态

使用 `emptyProps` 或 `empty` 插槽自定义空状态

<preview path="@docs/component/table/demos/state-empty.vue"></preview>
